engines:
  rubocop:
    enabled: true
    channel: rubocop-0-58
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 60
ratings:
  paths:
    - lib/**
exclude_paths:
  - spec/**/*
